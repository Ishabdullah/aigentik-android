<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <queries>
        <package android:name="com.google.android.gms" />
    </queries>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aigentik">

        <activity android:name=".ui.MainActivity"
            android:exported="true" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.OnboardingActivity"
            android:exported="false" android:label="Setup Aigentik"/>

        <activity android:name=".ui.ChatActivity"
            android:exported="false" android:label="Chat"/>

        <activity android:name=".ui.ModelManagerActivity"
            android:exported="false" android:label="AI Model"/>

        <activity android:name=".ui.SettingsActivity"
            android:exported="false" android:label="Settings"/>

        <activity android:name=".ui.RuleManagerActivity"
            android:exported="false" android:label="Rule Manager"/>

        <activity android:name=".ui.AiDiagnosticActivity"
            android:exported="false" android:label="AI Diagnostic"/>

        <!--
            foregroundServiceType="dataSync" is appropriate because AigentikService
            performs network operations (Gmail OAuth2 REST API calls) in the background.
            NOTE for future: Android may require "specialUse" for on-device AI inference
            workloads on future API levels. If Play Store distribution is ever added,
            review this type declaration against current Play policy and add a
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE" .../> entry.
            For sideloaded deployment this is not required.
        -->
        <service android:name=".core.AigentikService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>

        <service android:name=".adapters.NotificationAdapter"
            android:exported="true"
            android:label="Aigentik Message Listener"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService"/>
            </intent-filter>
            <meta-data
                android:name="android.app.notification_listener_settings"
                android:resource="@xml/notification_listener"/>
        </service>

        <!-- Boot receiver — restarts Aigentik after reboot -->
        <receiver android:name=".system.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </receiver>

    </application>
</manifest>
